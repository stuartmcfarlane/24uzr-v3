{
  "name": "24uzr-api",
  "version": "3.0.0",
   "engines": {
    "node": ">=20.0.0"
  },
  "main": "index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:w": "tsc -w -p tsconfig.json",
    "start:dev": "nodemon --watch build --watch ../.env --env-file=../.env build/index.js",
    "dev": "concurrently \"npm:build:w\" \"npm:start:dev\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@fastify/mysql": "^4.3.0",
    "fastify": "^4.28.1",
    "fastify-zod": "^1.4.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "concurrently": "^9.0.1",
    "nodemon": "^3.1.4",
    "typescript": "^5.5.4"
  }
}
